mixin purpose(purpose)
	+b.purpose&attributes(attributes)
		+e.descr 
			+e('h2').title
				|Какие у вас планы на будущее?
			+e('p').help 
				|Этот ответ ни на что не повлияет. 
				|Не беда, если в будущем ваши планы поменяются.
		+b.radiobtn
			+e('ul').list
				each item in purpose
					+e('li').item 
						+e.wrapper
							if item.checked 
								+e('input').input(type="#{item.type}" id="#{item.id}" name="#{item.name}"  checked)
							else
								+e('input').input(type="#{item.type}" id="#{item.id}" name="#{item.name}" )
							+e('label').label(for="#{item.id}") #{item.text}